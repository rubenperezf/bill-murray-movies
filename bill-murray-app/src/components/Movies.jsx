import React, { useEffect, useState} from 'react'
import axios from 'axios'

function Movies = props = {
    const { title, year, director, img_url, role, id } = props
    

        return (
            <h1>{title}</h1>
        )
    
}
export default Movies